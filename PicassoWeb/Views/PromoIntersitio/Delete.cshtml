@model PicassoWeb.Models.PromoIntersitio

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    }

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>PromoIntersitio</legend>

    <div class="display-label">Nombre</div>
    <div class="display-field">@Model.Nombre</div>

    <div class="display-label">Descripcion</div>
    <div class="display-field">@Model.Descripcion</div>

    <div class="display-label">DirImagen</div>
    <div class="display-field">@Model.DirImagen</div>

    <div class="display-label">Activo</div>
    <div class="display-field">@Model.Activo</div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}


