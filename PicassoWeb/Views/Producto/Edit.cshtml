@model PicassoWeb.Models.Producto

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    }

<h2>Editar</h2>


<script src="@Url.Content("~/Scripts/jquery.unobtrusive-ajax.js")" type="text/javascript"></script>
<div class="large-12 columns rigth">
    <div class="section-container auto" data-section>
        <section>
                <p class="title" data-section-title><a href="#p1">Generales</a></p>
                <div class="content" data-section-content>
                 @using (Html.BeginForm("Edit", "Producto",FormMethod.Post, new { @class = "custom",@enctype = "multipart/form-data" })) {
    @Html.ValidationSummary(true)

                        @Html.HiddenFor(model => model.Id)
                        @Html.Partial("_CreateOrEdit", Model)
                        <p>
                            <input type="submit" value="Guardar" class ="button" />
                        </p>
                }
            </section>
    </div>
</div>
<div>
    @Html.ActionLink("Volver", "Index")
</div>

